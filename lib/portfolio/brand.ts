/**
 * Accent orange taken from anikamantri.com. Used by the hero name and the site cursor. It is
 * deliberately not a global `--pf-*` token (the hero keeps its own palette).
 */
export const ACCENT_ORANGE = '#ED3801'
